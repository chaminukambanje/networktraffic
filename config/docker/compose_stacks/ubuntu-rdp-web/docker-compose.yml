services:
  guacamole:
    image: oznu/guacamole:latest
    container_name: ubuntu-rdp-web
    restart: unless-stopped
    ports:
      - "8084:8080"
    environment:
      - GUACD_LOG_LEVEL=info
    volumes:
      - ./config:/config
