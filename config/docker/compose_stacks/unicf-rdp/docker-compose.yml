services:
  guacamole:
    image: oznu/guacamole:latest
    container_name: unicaf-rdp-web
    restart: unless-stopped
    ports:
      - "8086:8080"
    environment:
      - GUACD_LOG_LEVEL=info
    volumes:
      - ./config:/config
